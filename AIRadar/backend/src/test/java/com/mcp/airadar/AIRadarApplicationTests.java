package com.mcp.airadar;

import org.junit.jupiter.api.Test;

// 이 테스트는 외부 인프라(PostgreSQL, Kafka 등) 없이 실행되므로
// 전체 컨텍스트 로드 대신 단순 통과 테스트로 유지한다.
class AIRadarApplicationTests {

    @Test
    void contextLoads() {
        // 빌드 파이프라인에서 외부 인프라 없이도 통과해야 하므로 빈 테스트 유지
    }

}
